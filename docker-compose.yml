# ==========================================================
# App de control de gastos
# Funciona igual en local y en un NAS Synology con Docker.
#
#   Local:     docker compose up -d --build
#   Synology:  igual, o desde Container Manager (Proyecto)
#
# Accede a la app en:  http://<host>:${APP_PORT}  (por defecto 28080)
# ==========================================================

services:
  db:
    image: mysql:8.0
    container_name: gastos_db
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      MYSQL_DATABASE: ${MYSQL_DATABASE}
      MYSQL_USER: ${MYSQL_USER}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD}
    command: --character-set-server=utf8mb4 --collation-server=utf8mb4_unicode_ci
    volumes:
      - db_data:/var/lib/mysql
      - ./db/init:/docker-entrypoint-initdb.d:ro
    ports:
      - "${DB_EXTERNAL_PORT:-3307}:3306"
    healthcheck:
      # Usa la contraseña del contenedor ($$ evita que Compose la sustituya al crear el proyecto).
      # start_period largo: MySQL en NAS puede tardar más en la primera inicialización.
      test: ["CMD-SHELL", "mysqladmin ping -h 127.0.0.1 -u root -p\"$$MYSQL_ROOT_PASSWORD\" --silent || exit 1"]
      interval: 15s
      timeout: 10s
      retries: 20
      start_period: 90s

  backend:
    build:
      context: ./backend
    container_name: gastos_backend
    restart: unless-stopped
    environment:
      DB_HOST: db
      DB_PORT: 3306
      DB_NAME: ${MYSQL_DATABASE}
      DB_USER: ${MYSQL_USER}
      DB_PASS: ${MYSQL_PASSWORD}
      JWT_SECRET: ${JWT_SECRET}
      ADMIN_USERNAME: ${ADMIN_USERNAME}
      ADMIN_PASSWORD: ${ADMIN_PASSWORD}
    depends_on:
      db:
        condition: service_healthy

  frontend:
    build:
      context: ./frontend
    container_name: gastos_frontend
    restart: unless-stopped
    ports:
      - "${APP_PORT:-28080}:80"
    depends_on:
      - backend

volumes:
  db_data:
